require 'spec_helper'

describe GitShortener do
  it 'has a version number' do
    expect(GitShortener::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
